---
import { Image } from "astro:assets";
import defaultImage from "../assets/images/default-image.avif";

interface Props {
  img: () => Promise<{
    default: ImageMetadata;
  }>;
  alt: string;
  class?: string;
}
const { alt, img, class: className, ...rest } = Astro.props;
---

<Image
  src={img?.() || defaultImage}
  class={`img-float ${className}`}
  alt={alt}
  loading="lazy"
  decoding="async"
  {...rest}
/>

