---
import type { Technology } from "../interfaces/types";
import { SkillItem } from "./";

interface Props {
  subtitle: string;
  skills: Technology[];
}
const { subtitle, skills } = Astro.props;
---

<>
  <h3 class="skill__subtitle">{subtitle}</h3>
  <div class="skill__container">
    {skills.map((skill) => <SkillItem {...skill} />)}
  </div>
</>

<style>
  .skill__subtitle {
    color: var(--text-color);
    font-weight: 600;
    font-size: var(--text-md);
    margin-left: 3rem;
  }
  .skill__container {
    display: flex;
    flex-wrap: wrap;
    text-align: center;
    justify-content: space-around;
    padding: var(--padding-vertical);
  }
  @media (min-width: 768px) {
    .skill__container {
      justify-content: center;
      gap: 45px;
    }
  }
</style>
